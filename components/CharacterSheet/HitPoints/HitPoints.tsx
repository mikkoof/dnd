import styles from "../../../styles/Sheet.module.css"

const HitPointsBox = () => {
  return (
    <div className={`${styles.hitPointsBox} ${styles.cell}`}>

    </div>
  )
}

export default HitPointsBox;